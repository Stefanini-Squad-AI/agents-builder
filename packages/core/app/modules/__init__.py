"""Agents Workshop modules.

Modules are optional feature packages that extend the core functionality.
Each module is self-contained with its own routes, models, and services.

Available modules:
- migration_workbench: ETL migration support (SSIS → Databricks, etc.)
"""
